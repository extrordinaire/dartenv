name: dartenv
description: A Dart Tool that makes it way easier to work with .env files.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ">=2.19.3 <3.0.0"

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0

scripts:
  test: dart test/dartenv_test.dart
  format: dartfmt -w .
  ci: dart run test && dart run format
